<header class="bg-white shadow-md">
    <div class="flex justify-between items-center py-2 px-5 border-b-2 border-gray-200">
        <div class="flex items-center">
            <h1 class="text-2xl font-bold">SEFI ❤️</h1>
        </div>
        <div class="flex items-center space-x-2">
            <a href="#" class="px-2 py-2 bg-gradient-to-r from-gray-400 to-gray-600 text-white font-semibold rounded-lg text-sm">Registro</a>
            <a href="#" class="px-2 py-2 bg-gradient-to-r from-gray-400 to-gray-600 text-white font-semibold rounded-lg text-sm">Contáctame</a>
        </div>
    </div>
</header>


      <% if current_user %>
        <%= link_to "logout", destroy_user_session_path, data: { turbo_method: :delete } %>
        <p>Current user: <%= current_user&.email%></p>
      <% else %>
        <%= link_to "login", user_session_path %>
      <% end %>