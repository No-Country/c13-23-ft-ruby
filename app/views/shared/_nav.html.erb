<header class="bg-white dark:bg-slate-800 max-w-full border border-gray-200">
  <div class="flex justify-between items-center py-2 px-5">
    <div class="flex items-center">
      <h1 class="text-2xl font-bold">LOGO ❤️</h1>
    </div>

    <!-- Dropdown -->
    <button class="btn-sefi">

      <span>
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
        </svg>
      </span>

    </button>
</header>

<!--
      <% if current_user %>
        <%= link_to "logout", destroy_user_session_path, data: { turbo_method: :delete } %>
        <p>Current user: <%= current_user&.email%></p>
      <% else %>
        <%= link_to "login", user_session_path %>
      <% end %>

      <a href="#" class="px-2 py-2 font-semibold rounded-lg text-sm">Registro</a>
            <a href="#" class="px-2 py-2 bg-gradient-to-r from-gray-400 to-gray-600 text-white font-semibold rounded-lg text-sm">Contáctame</a>
        </div>
-->

