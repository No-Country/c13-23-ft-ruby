<!DOCTYPE html>
<html>
  <head>
    <title>Sefi ❤️</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <% if current_user %>
      <%= render "shared/nav-dash" %>
    <% else %>
      <%= render "shared/nav" unless request.fullpath == '/users/sign_in' || request.fullpath == '/users/sign_up' %>
    <% end %>
    <%= yield %>
    <%= render "shared/footer" unless request.fullpath == '/users/sign_in' || request.fullpath == '/users/sign_up'%>
  </body>
</html>
